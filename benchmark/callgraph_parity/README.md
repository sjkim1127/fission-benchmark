# Call-graph parity

**Status:** extension track (active).

Callee address sets from Ghidra call references vs Fission disasm CALL targets.

```bash
python -m benchmark.callgraph_parity.run --limit 10
```
