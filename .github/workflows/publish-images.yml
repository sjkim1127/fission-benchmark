# Builds and pushes decompiler Docker images to ghcr.io when their
# Dockerfile or server.py changes on main. Benchmark jobs then pull
# pre-built images instead of re-compiling from scratch every run.
name: Publish Images

on:
  push:
    branches: [main]
    paths:
      - 'docker/fission/**'
      - 'docker/ghidra/**'
      - 'docker/boomerang/**'
      - 'docker/radare2/**'
      - 'docker/angr/**'
      - 'docker/snowman/**'
      - 'docker/revng/**'
      - 'docker/reko/**'
      - 'docker/retdec/**'
      - 'docker/oracle/**'
      - 'runner/differential_oracle.py'
      - 'runner/semantic.py'
  workflow_dispatch:
    inputs:
      services:
        description: "Comma-separated services to rebuild (e.g. fission,ghidra)"
        type: string
        default: "fission,ghidra,boomerang,radare2,angr,snowman,revng,reko,retdec,oracle"
      fission_version:
        description: "Fission release tag to bake into the fission image"
        type: string
        default: "v0.1.4"
  repository_dispatch:
    types: [fission-release]

env:
  REGISTRY: ghcr.io
  IMAGE_PREFIX: ghcr.io/${{ github.repository_owner }}/fission-benchmark
  # Published images always bake a GitHub Release Fission CLI (never local mount).
  FISSION_SOURCE: release
  FISSION_VERSION: ${{ github.event.client_payload.fission_version || github.event.inputs.fission_version || 'v0.1.4' }}

permissions:
  contents: read
  packages: write

jobs:
  detect-changes:
    name: Detect changed services
    runs-on: ubuntu-latest
    outputs:
      services: ${{ steps.detect.outputs.services }}
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 2

      - name: Detect changed services
        id: detect
        run: |
          if [ "${{ github.event_name }}" = "repository_dispatch" ]; then
            SERVICES='["fission"]'
          elif [ "${{ github.event_name }}" = "workflow_dispatch" ]; then
            INPUT="${{ github.event.inputs.services }}"
            # Convert comma-separated input to JSON array
            SERVICES=$(echo "$INPUT" | tr ',' '\n' | jq -R -s -c 'split("\n") | map(select(length > 0))')
          else
            CHANGED=$(git diff --name-only HEAD~1 HEAD | grep '^docker/' | sed 's|docker/||;s|/.*||' | sort -u || true)
            if git diff --name-only HEAD~1 HEAD | grep -Eq '^runner/(differential_oracle|semantic)\.py$'; then
              CHANGED=$(printf '%s\noracle\n' "$CHANGED" | sed '/^$/d' | sort -u)
            fi
            if [ -z "$CHANGED" ]; then
              SERVICES='[]'
            else
              SERVICES=$(echo "$CHANGED" | jq -R -s -c 'split("\n") | map(select(length > 0))')
            fi
          fi
          echo "services=$SERVICES" >> "$GITHUB_OUTPUT"
          echo "Will build: $SERVICES"

  publish:
    name: Build & push ${{ matrix.service }}
    needs: detect-changes
    if: ${{ needs.detect-changes.outputs.services != '[]' && needs.detect-changes.outputs.services != '' }}
    runs-on: ubuntu-latest
    timeout-minutes: 45
    strategy:
      fail-fast: false
      matrix:
        service: ${{ fromJson(needs.detect-changes.outputs.services) }}
    steps:
      - uses: actions/checkout@v4

      - name: Log in to GitHub Container Registry
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Resolve image metadata
        id: image-meta
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          set -euo pipefail
          svc="${{ matrix.service }}"
          fission_version="${FISSION_VERSION:-v0.1.4}"
          if [ "$svc" = "fission" ] && [ "$fission_version" = "latest" ]; then
            fission_version="$(gh api repos/sjkim1127/Fission/releases/latest --jq .tag_name)"
          fi

          {
            echo "fission_version=$fission_version"
            echo "tags<<EOF"
            echo "${IMAGE_PREFIX}-${svc}:latest"
            echo "${IMAGE_PREFIX}-${svc}:${GITHUB_SHA}"
            if [ "$svc" = "fission" ]; then
              echo "${IMAGE_PREFIX}-${svc}:${fission_version}"
            fi
            echo "EOF"
          } >> "$GITHUB_OUTPUT"
          echo "Image ${svc} tags:"
          sed -n '/tags<<EOF/,/EOF/p' "$GITHUB_OUTPUT"

      - name: Cache Docker layers
        uses: actions/cache@v4
        with:
          path: /tmp/.buildx-cache-${{ matrix.service }}
          key: buildx-${{ matrix.service }}-${{ hashFiles(format('docker/{0}/**', matrix.service)) }}-${{ steps.image-meta.outputs.fission_version }}
          restore-keys: |
            buildx-${{ matrix.service }}-

      - name: Build and push ${{ matrix.service }}
        uses: docker/build-push-action@v6
        with:
          context: ${{ matrix.service == 'oracle' && '.' || format('./docker/{0}', matrix.service) }}
          file: ${{ matrix.service == 'oracle' && './docker/oracle/Dockerfile' || format('./docker/{0}/Dockerfile', matrix.service) }}
          push: true
          tags: ${{ steps.image-meta.outputs.tags }}
          build-args: |
            FISSION_SOURCE=release
            FISSION_VERSION=${{ steps.image-meta.outputs.fission_version }}
          cache-from: type=local,src=/tmp/.buildx-cache-${{ matrix.service }}
          cache-to: type=local,dest=/tmp/.buildx-cache-${{ matrix.service }}-new,mode=max

      - name: Move cache (keep slim)
        run: |
          rm -rf /tmp/.buildx-cache-${{ matrix.service }}
          mv /tmp/.buildx-cache-${{ matrix.service }}-new /tmp/.buildx-cache-${{ matrix.service }}

      - name: Image digest
        run: echo "Pushed ${{ env.IMAGE_PREFIX }}-${{ matrix.service }}:latest"

  # GITHUB_TOKEN cannot re-fire sibling workflows from a bare tag/API side-effect.
  # After a successful bake (release dispatch or manual), chain Benchmark via gh CLI.
  chain-benchmark:
    name: Chain Benchmark (gh CLI)
    needs: publish
    if: >
      always()
      && needs.publish.result == 'success'
      && (github.event_name == 'repository_dispatch' || github.event_name == 'workflow_dispatch')
    runs-on: ubuntu-latest
    permissions:
      actions: write
      contents: read
    steps:
      - name: Trigger Benchmark & Deploy after image bake
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        shell: bash
        run: |
          set -euo pipefail
          ver="${{ github.event.client_payload.fission_version || github.event.inputs.fission_version || 'v0.1.4' }}"
          if [ "$ver" = "latest" ]; then
            ver="$(gh api repos/sjkim1127/Fission/releases/latest --jq .tag_name)"
          fi
          echo "Dispatching Benchmark & Deploy for FISSION_VERSION=${ver}"
          # Same-repo workflow_dispatch chains with GITHUB_TOKEN (unlike on.push).
          # Ranking path: fission+ghidra only (~4–5× fewer cells than 9-tool).
          gh workflow run "Benchmark & Deploy" \
            --repo "${{ github.repository }}" \
            -f "fission_version=${ver}" \
            -f "corpus=dev" \
            -f "run_mode=official" \
            -f "publish_results=true" \
            -f "matrix_profile=core_c_pe" \
            -f "decompilers=fission,ghidra" \
            -f "benchmark_limit=0" \
            -f "benchmark_variant_limit=0" \
            -f "parity_limit=40"
          echo "Queued. Track with: gh run list --workflow 'Benchmark & Deploy' --limit 5"
