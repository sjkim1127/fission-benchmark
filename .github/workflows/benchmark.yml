# Full benchmark run + GitHub Pages deployment.
# Runs on: push to main, weekly schedule, manual dispatch.
# Images are pulled from ghcr.io (pre-built by publish-images.yml).
# Falls back to local build if an image is not yet published.
name: Benchmark & Deploy

env:
  # Official / Pages contract: only GitHub Release bundles. Never FISSION_SOURCE=local.
  FISSION_SOURCE: release
  FISSION_VERSION: ${{ github.event.client_payload.fission_version || github.event.inputs.fission_version || 'v0.1.4' }}
  FISSION_ASSETS_TAG: assets-v1
  CORPUS_TARGET: windows-x86_64
  CORPUS_SPLIT: ${{ github.event.inputs.corpus || 'dev' }}
  # Tiered decompiler sets (wall-clock ≈ cells × Ghidra cost):
  #   ranking / release / weekly full_matrix: fission+ghidra (default)
  #   push smoke: full 9-tool on small smoke profile (multi-decomp dashboard envelope)
  #   optional multi snapshot: pass decompilers=… all 9 explicitly
  BENCHMARK_DECOMPILERS: ${{ github.event.inputs.decompilers || (github.event_name == 'push' && 'fission,ghidra,boomerang,radare2,angr,snowman,revng,reko,retdec' || 'fission,ghidra') }}
  # Push → smoke; weekly → full_matrix (2-tool parallel fan-out); else core_c_pe ranking.
  BENCHMARK_PROFILE: ${{ github.event.inputs.matrix_profile || (github.event_name == 'push' && 'smoke' || (github.event_name == 'schedule' && 'full_matrix' || 'core_c_pe')) }}
  # Limits default to 0 when a profile selects the slice (smoke/core_c_pe).
  BENCHMARK_LIMIT: ${{ github.event.inputs.benchmark_limit || '0' }}
  BENCHMARK_VARIANT_LIMIT: ${{ github.event.inputs.benchmark_variant_limit || '0' }}
  RUN_MODE: ${{ github.event.inputs.run_mode || (github.event_name == 'push' && 'smoke' || 'official') }}
  PUBLISH_RESULTS: ${{ github.event.inputs.publish_results || (github.event_name == 'push' && 'false' || 'true') }}
  # Parity capped on ranking path (not full corpus); full only when operator sets 0.
  PARITY_LIMIT: ${{ github.event.inputs.parity_limit || (github.event_name == 'push' && '20' || '40') }}
  PARITY_DECOMPILERS: ${{ github.event.inputs.parity_decompilers || 'fission,ghidra' }}
  PARITY_REQUEST_TIMEOUT: ${{ github.event.inputs.parity_timeout || '180' }}
  # Conservative reliability: strict only, no leniency in CI.
  PARITY_CANONICALIZE_MODE: strict
  RELIABILITY_POLICY: conservative
  PARITY_BINARY_WORKERS: "3"
  GHIDRA_HEADLESS_WORKERS: "3"
  FISSION_CLI_WORKERS: "4"
  # Per-function CLI budget inside fission adapter (ms). 30s was too low for
  # core_c_pe hard rows under load (preview_timeout storms → coverage < 0.90).
  FISSION_DECOMP_TIMEOUT_MS: "120000"
  # I/O-bound HTTP batches. Keep moderate: too high floods fission/ghidra and
  # produces batch adapter_error storms that drop coverage below 0.90.
  BENCHMARK_HTTP_CONCURRENCY: "6"
  FANOUT_PARALLEL: "2"
  REGISTRY: ghcr.io
  IMAGE_PREFIX: ghcr.io/${{ github.repository_owner }}/fission-benchmark
  PYTHONPATH: .

on:
  push:
    branches: [main]
    # Docs-only pushes do not need a 15–25m multi-decompiler run.
    paths-ignore:
      - '**.md'
      - 'docs/**'
      - 'LICENSE'
  workflow_dispatch:
    inputs:
      fission_version:
        description: "Fission release tag to benchmark"
        type: string
        default: "v0.1.4"
      corpus:
        description: "Corpus split to benchmark"
        type: choice
        options: [dev, holdout]
        default: dev
      decompilers:
        description: "Comma-separated decompilers (default ranking: fission,ghidra; use full 9 for multi-decomp UI)"
        type: string
        default: "fission,ghidra"
      benchmark_limit:
        description: "Function limit for dev/holdout benchmark; use 0 for full corpus"
        type: string
        default: "0"
      benchmark_variant_limit:
        description: "Compiler variants per function; use 0 for all variants"
        type: string
        default: "0"
      parity_limit:
        description: "Parity subject limit; 0=full. Ranking default 40; weekly multi 0"
        type: string
        default: "40"
      publish_results:
        description: "Commit results and deploy GitHub Pages"
        type: boolean
        default: false
      run_mode:
        description: "Execution mode (smoke, local, official)"
        type: choice
        options: [smoke, local, official]
        default: smoke
      matrix_profile:
        description: "Corpus matrix profile (corpus/matrix/profiles.yaml)"
        type: choice
        options: [smoke, core_c_pe, opt_cliff, lang_cpp, lang_rust, lang_go, multi_isa, full_matrix]
        default: core_c_pe
  # Note: fission-release repository_dispatch is handled by Publish Images first
  # (bake GHCR), which then chains this workflow via `gh workflow run`.
  # Keeping dispatch here would race the image bake. Use:
  #   gh workflow run "Publish Images" -f fission_version=vX.Y.Z
  # or repository_dispatch event_type=fission-release on this repo.
  schedule:
    - cron: '0 0 * * 0'  # Every Sunday midnight UTC

permissions:
  contents: write
  packages: read

concurrency:
  # Include profile so official core_c_pe is not cancelled by push smoke.
  group: benchmark-${{ github.ref }}-${{ github.event.inputs.matrix_profile || github.event_name }}
  cancel-in-progress: true

jobs:
  preflight:
    name: Check benchmark currency
    runs-on: ubuntu-latest
    outputs:
      is_current: ${{ steps.current.outputs.is_current }}
    steps:
      - id: current
        name: Check current branch head
        run: |
          set -eu
          remote_ref="refs/heads/${GITHUB_REF_NAME:-main}"
          current_sha="$(git ls-remote "https://github.com/${GITHUB_REPOSITORY}.git" "$remote_ref" | awk '{print $1}')"
          if [ -z "$current_sha" ] || [ "$GITHUB_SHA" = "$current_sha" ]; then
            echo "is_current=true" >> "$GITHUB_OUTPUT"
            echo "Current benchmark run for ${GITHUB_SHA}"
          else
            echo "is_current=false" >> "$GITHUB_OUTPUT"
            echo "Stale benchmark run: ${GITHUB_SHA}; current ${remote_ref} is ${current_sha}"
          fi

  run-benchmark:
    name: Run benchmark
    needs: preflight
    if: ${{ needs.preflight.outputs.is_current == 'true' }}
    outputs:
      is_current: ${{ steps.current.outputs.is_current }}
      should_publish: ${{ steps.publish-mode.outputs.should_publish }}
    runs-on: ubuntu-latest
    # full_matrix fan-out needs headroom; smoke/core stay well under this.
    timeout-minutes: ${{ (github.event.inputs.matrix_profile == 'full_matrix' || github.event_name == 'schedule') && 360 || 150 }}
    steps:
      - id: current
        name: Export current-run state
        run: echo "is_current=${{ needs.preflight.outputs.is_current }}" >> "$GITHUB_OUTPUT"

      - uses: actions/checkout@v4
        with:
          fetch-depth: 2

      - name: Log in to GitHub Container Registry
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Resolve Fission release version
        id: fission-version
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          set -euo pipefail
          requested="${FISSION_VERSION:-v0.1.4}"
          if [ "$requested" = "latest" ]; then
            resolved="$(gh api repos/sjkim1127/Fission/releases/latest --jq .tag_name)"
          else
            resolved="$requested"
          fi
          echo "Resolved Fission version: $resolved"
          echo "FISSION_VERSION=$resolved" >> "$GITHUB_ENV"
          echo "version=$resolved" >> "$GITHUB_OUTPUT"

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Wait for GHCR fission image (release bake)
        if: github.event_name != 'push'
        env:
          GH_TOKEN: ${{ github.token }}
        shell: bash
        run: |
          set -euo pipefail
          remote="${IMAGE_PREFIX}-fission:${FISSION_VERSION}"
          echo "Waiting for ${remote} (up to ~15m)..."
          for i in $(seq 1 30); do
            if docker pull "${remote}"; then
              echo "✓ Available after attempt ${i}"
              exit 0
            fi
            echo "  miss ${i}/30 — sleep 30s"
            sleep 30
          done
          echo "⚠ Image still missing after wait; local build fallback will run"
          exit 0

      - name: Pull or build decompiler images
        run: |
          set -euo pipefail
          SERVICES="fission ghidra boomerang radare2 angr snowman revng reko retdec oracle"
          CHANGED_SERVICES=""
          if [ "${GITHUB_EVENT_NAME}" = "push" ]; then
            CHANGED_SERVICES="$(git diff --name-only HEAD~1 HEAD | awk -F/ '/^docker\// {print $2}' | sort -u | tr '\n' ' ')"
            # Oracle bakes runner/ at image build time; harness-only commits must
            # not silently keep a stale ghcr oracle (even with volume mount).
            if git diff --name-only HEAD~1 HEAD | grep -Eq '^runner/(differential_oracle|semantic)\.py$|^docker-compose\.yml$'; then
              CHANGED_SERVICES="$(printf '%s oracle\n' "$CHANGED_SERVICES" | tr ' ' '\n' | sed '/^$/d' | sort -u | tr '\n' ' ')"
            fi
            echo "Docker services changed in this push: ${CHANGED_SERVICES:-<none>}"
          fi
          mkdir -p /tmp/img-status

          pull_one() {
            local svc="$1"
            local remote tag
            if [ "$svc" = "fission" ]; then
              remote="${IMAGE_PREFIX}-fission:${FISSION_VERSION}"
              tag="fission-benchmark-fission"
            else
              remote="${IMAGE_PREFIX}-${svc}:latest"
              tag="fission-benchmark-${svc}"
            fi
            if echo " ${CHANGED_SERVICES} " | grep -q " ${svc} "; then
              echo "build" > "/tmp/img-status/${svc}"
              echo "==> ${svc}: changed in push — queue local build"
              return 0
            fi
            echo "==> Trying to pull ${remote}..."
            if docker pull "${remote}"; then
              docker tag "${remote}" "${tag}"
              echo "ok" > "/tmp/img-status/${svc}"
              echo "  ✓ Pulled ${svc}"
            else
              echo "miss" > "/tmp/img-status/${svc}"
              echo "  ⚠  Pull miss ${svc} — will build locally"
            fi
          }
          export -f pull_one
          export IMAGE_PREFIX FISSION_VERSION CHANGED_SERVICES

          # Phase 1: parallel pulls (I/O bound; was ~3–5m sequential).
          printf '%s\n' $SERVICES | xargs -P 6 -n 1 bash -c 'pull_one "$@"' _

          # Phase 2: sequential builds for changed/missed images only.
          for svc in $SERVICES; do
            st="$(cat "/tmp/img-status/${svc}" 2>/dev/null || echo miss)"
            if [ "$st" = "ok" ]; then
              continue
            fi
            echo "==> Building ${svc} locally (status=${st})..."
            if [ "$svc" = "fission" ]; then
              docker compose build --pull fission
            else
              docker compose build "${svc}"
            fi
          done

      - uses: actions/setup-python@v5
        with:
          python-version: "3.11"
          cache: pip
          cache-dependency-path: pyproject.toml

      - name: Install Python deps
        run: pip install -e ".[dev]"

      - name: Cache corpus binaries + synced manifests
        id: corpus-cache
        uses: actions/cache@v4
        with:
          # build_matrix.py rewrites manifest addrs from nm. Cache binaries +
          # manifests together so PE layout stays consistent after restore.
          path: |
            corpus/dev/binaries
            corpus/holdout/binaries
            corpus/dev/manifests
            corpus/holdout/manifests
            corpus/dev/inventory.json
            corpus/holdout/inventory.json
          key: corpus-bin-manifest-v3-${{ hashFiles('corpus/**/source/**', 'corpus/**/manifests/**', 'corpus/matrix/**', 'docker/corpus-builder/**', 'scripts/build_matrix.py', 'scripts/build_corpus.py') }}

      - name: Build corpus binaries
        if: steps.corpus-cache.outputs.cache-hit != 'true'
        run: docker compose --profile tools run --rm --build corpus-builder

      - name: Verify corpus binaries present
        run: bash scripts/verify_corpus_binaries.sh

      - name: Start decompiler containers
        run: |
          set -euo pipefail
          docker compose up -d fission ghidra boomerang radare2 angr snowman revng reko retdec oracle

          wait_port() {
            local port="$1"
            local i
            for i in $(seq 1 60); do
              if curl -sf "http://localhost:${port}/health" >/dev/null; then
                echo "ready :${port}"
                return 0
              fi
              sleep 2
            done
            echo "Service on port ${port} did not become healthy" >&2
            return 1
          }
          export -f wait_port

          # Parallel health waits — sequential 5s polls stacked ~minutes of idle.
          if ! printf '%s\n' 8000 8001 8002 8003 8004 8005 8006 8008 8009 8010 \
            | xargs -P 10 -n 1 bash -c 'wait_port "$@"' _; then
            docker compose ps
            docker compose logs --no-color
            exit 1
          fi

      - name: Verify Fission container version
        run: |
          python -c '
          import json, os, urllib.request
          expected = os.environ["FISSION_VERSION"].lstrip("v")
          health = json.load(urllib.request.urlopen("http://localhost:8000/health", timeout=10))
          actual = str(health.get("version", ""))
          release = str(health.get("release_version", ""))
          source = str(health.get("source", "release"))
          print(f"Fission health version: {actual}")
          print(f"Fission release version: {release}")
          print(f"Fission source: {source}")
          assert source == "release", f"CI must use release source, got {source!r}"
          assert not str(release).startswith("local"), f"CI must not run local bundle: {release}"
          assert not expected or release.lstrip("v") == expected, f"Fission image release mismatch: expected {expected}, got {release}"
          print("✓ Version matches (release)")
          '

      - name: Run dev benchmark
        # smoke ~40m; core_c_pe ~90–120m; full_matrix fan-out up to ~5h.
        timeout-minutes: ${{ env.BENCHMARK_PROFILE == 'full_matrix' && 300 || (github.event_name == 'push' && 40 || 120) }}
        env:
          ORACLE_ENDPOINT: http://localhost:8010
          BENCHMARK_PROFILE: ${{ env.BENCHMARK_PROFILE }}
        run: |
          set -euo pipefail
          if [ "${BENCHMARK_PROFILE:-}" = "full_matrix" ]; then
            echo "full_matrix fan-out (core ranking + language/ISA slices, parallel)"
            chmod +x scripts/run_matrix_fanout.sh
            # Language/ISA slices always 2-tool. Core uses requested set (ranking default 2).
            CORE_DECOMPILERS="${BENCHMARK_DECOMPILERS:-fission,ghidra}" \
            SLICE_DECOMPILERS="fission,ghidra" \
            RUN_MODE="$RUN_MODE" \
            FANOUT_PARALLEL="${FANOUT_PARALLEL:-2}" \
            FANOUT_PROFILES="core_c_pe lang_cpp lang_rust lang_go multi_isa" \
              ./scripts/run_matrix_fanout.sh dev results/dev_latest.json
            # Publication ranking artifact: pure core_c_pe slice.
            if [ -f results/dev_core_c_pe.json ]; then
              cp results/dev_core_c_pe.json results/dev_publish.json
            else
              cp results/dev_latest.json results/dev_publish.json
            fi
          else
            EXTRA_ARGS=""
            if [ -n "$BENCHMARK_LIMIT" ] && [ "$BENCHMARK_LIMIT" != "0" ]; then
              EXTRA_ARGS="$EXTRA_ARGS --limit $BENCHMARK_LIMIT"
            fi
            if [ -n "$BENCHMARK_VARIANT_LIMIT" ] && [ "$BENCHMARK_VARIANT_LIMIT" != "0" ]; then
              EXTRA_ARGS="$EXTRA_ARGS --variant-limit $BENCHMARK_VARIANT_LIMIT"
            fi
            if [ -n "$BENCHMARK_PROFILE" ]; then
              EXTRA_ARGS="$EXTRA_ARGS --profile $BENCHMARK_PROFILE"
            fi
            echo "Running with BENCHMARK_PROFILE=${BENCHMARK_PROFILE:-<none>} EXTRA_ARGS=$EXTRA_ARGS"
            python runner/runner.py --corpus dev $EXTRA_ARGS --decompilers "$BENCHMARK_DECOMPILERS" --run-mode "$RUN_MODE" --output results/dev_latest.json
            cp results/dev_latest.json results/dev_publish.json
          fi

      # -----------------------------------------------------------------------
      # Fission validity gate — abort the publish path when every Fission row
      # results/latest.* or be deployed to GitHub Pages.
      # The validity logic lives in runner/run_validity.py (shared with report.py)
      # so the CI gate and the dashboard banner are always in agreement.
      # -----------------------------------------------------------------------
      - name: Validate Fission coverage gate
        id: fission-gate
        run: |
          # Gate publication on the ranking slice (core_c_pe under full_matrix).
          GATE_JSON=results/dev_publish.json
          if [ ! -f "$GATE_JSON" ]; then
            GATE_JSON=results/dev_latest.json
          fi
          python -m runner.run_validity "$GATE_JSON" \
            --github-env     "$GITHUB_ENV" \
            --github-summary "$GITHUB_STEP_SUMMARY"
          # Standard-set contract (summary + taxonomy denominators + semantic-only correctness)
          python scripts/check_benchmark_path.py "$GATE_JSON"
          # Multi-lang merged envelope is diagnostic; still surface validity if present.
          if [ -f results/dev_latest.json ] && [ "$(realpath results/dev_latest.json)" != "$(realpath "$GATE_JSON")" ]; then
            echo "Merged full_matrix envelope present (diagnostic):"
            python -m runner.run_validity results/dev_latest.json || true
          fi

      - name: Run holdout benchmark
        # Skip if validity gate failed — holdout on an invalid run wastes CI time.
        if: steps.fission-gate.outcome == 'success'
        timeout-minutes: ${{ github.event_name == 'push' && 40 || 120 }}
        env:
          ORACLE_ENDPOINT: http://localhost:8010
          BENCHMARK_PROFILE: ${{ env.BENCHMARK_PROFILE }}
        run: |
          if ! find corpus/holdout/manifests -name '*.json' -print -quit | grep -q .; then
            echo "No holdout manifests found; writing empty holdout result set."
            mkdir -p results
            # YAML `|` strips common indent; keep body indented so the block stays valid.
            # Heredoc terminator must share that indent so bash sees bare `PY` after strip.
            python3 <<'PY'
          import json
          from pathlib import Path
          Path("results/holdout_latest.json").write_text(
              json.dumps(
                  {
                      "schema_version": 2,
                      "run": {
                          "status": "skipped",
                          "skip_reason": "no_holdout_manifests",
                          "corpus": "holdout",
                          "official": False,
                      },
                      "toolchain": {},
                      "matrix": {
                          "expected_decompilers": [],
                          "expected_cells": [],
                          "expected_rows": 0,
                          "observed_rows": 0,
                      },
                      "oracle": {"mode": "example_cases", "valid": False},
                      "validity": {
                          "valid": False,
                          "publishable": False,
                          "reasons": ["no_result_rows"],
                      },
                      "rows": [],
                  },
                  indent=2,
              )
              + "\n",
              encoding="utf-8",
          )
          PY
            if [ "$PUBLISH_RESULTS" = "true" ]; then
              echo "Official publication requires a non-empty holdout corpus."
              exit 1
            fi
            exit 0
          fi
          EXTRA_ARGS=""
          if [ -n "$BENCHMARK_LIMIT" ] && [ "$BENCHMARK_LIMIT" != "0" ]; then
            EXTRA_ARGS="$EXTRA_ARGS --limit $BENCHMARK_LIMIT"
          fi
          if [ -n "$BENCHMARK_VARIANT_LIMIT" ] && [ "$BENCHMARK_VARIANT_LIMIT" != "0" ]; then
            EXTRA_ARGS="$EXTRA_ARGS --variant-limit $BENCHMARK_VARIANT_LIMIT"
          fi
          # Holdout is C-centric; always use core ranking profile under full_matrix.
          HOLD_PROFILE="${BENCHMARK_PROFILE:-}"
          if [ "$HOLD_PROFILE" = "full_matrix" ]; then
            HOLD_PROFILE="core_c_pe"
          fi
          if [ -n "$HOLD_PROFILE" ]; then
            EXTRA_ARGS="$EXTRA_ARGS --profile $HOLD_PROFILE"
          fi
          python runner/runner.py --corpus holdout $EXTRA_ARGS --decompilers "$BENCHMARK_DECOMPILERS" --run-mode "$RUN_MODE" --output results/holdout_latest.json

      - name: Run overfitting check
        if: ${{ steps.fission-gate.outcome == 'success' && env.PUBLISH_RESULTS == 'true' }}
        run: |
          PUB_DEV=results/dev_publish.json
          if [ ! -f "$PUB_DEV" ]; then PUB_DEV=results/dev_latest.json; fi
          python runner/holdout_report.py \
            --dev "$PUB_DEV" \
            --holdout results/holdout_latest.json \
            --output results/overfitting_report.md \
            --json-output results/overfitting_report.json

      - name: Evaluate final publication evidence
        if: ${{ steps.fission-gate.outcome == 'success' && env.PUBLISH_RESULTS == 'true' }}
        run: |
          set -euo pipefail
          # Structural readiness (holdout manifests, non-empty rows, overfitting).
          PUB_DEV=results/dev_publish.json
          if [ ! -f "$PUB_DEV" ]; then PUB_DEV=results/dev_latest.json; fi
          python scripts/check_publication_ready.py \
            --dev "$PUB_DEV" \
            --holdout results/holdout_latest.json \
            --overfitting results/overfitting_report.json \
            --require-official
          # Full evidence-linked gate (always on core ranking slice).
          python -m runner.publication_gate \
            --dev "$PUB_DEV" \
            --holdout results/holdout_latest.json \
            --overfitting results/overfitting_report.json \
            --output results/publication-verdict.json \
            --github-env "$GITHUB_ENV"

      - name: Determine publish mode
        id: publish-mode
        if: always()
        run: |
          if [ "$PUBLISH_RESULTS" = "true" ] && [ "$MEASUREMENT_VALID" = "true" ] && [ "$RUN_PUBLISHABLE" = "true" ]; then
            echo "should_publish=true" >> "$GITHUB_OUTPUT"
          else
            echo "should_publish=false" >> "$GITHUB_OUTPUT"
          fi

      - name: Restore published dashboard from dev results
        # Only update latest.* when measurement is valid AND run is publishable.
        if: ${{ env.MEASUREMENT_VALID == 'true' && steps.publish-mode.outputs.should_publish == 'true' }}
        run: |
          PUB_DEV=results/dev_publish.json
          if [ ! -f "$PUB_DEV" ]; then PUB_DEV=results/dev_latest.json; fi
          python runner/render_report.py \
            --input "$PUB_DEV" \
            --corpus dev \
            --publication-verdict results/publication-verdict.json \
            --update-latest
          # Keep multi-lang merged envelope on the side for dashboard consumers.
          if [ -f results/dev_latest.json ] && [ "$(realpath results/dev_latest.json)" != "$(realpath "$PUB_DEV")" ]; then
            cp results/dev_latest.json results/dev_full_matrix.json
            echo "Preserved multi-lang merge as results/dev_full_matrix.json"
          fi
          python -c 'from pathlib import Path; from runner.artifact_integrity import verify_artifact_manifest; verify_artifact_manifest(Path("."))'

      - name: Run parity benchmarks (Ghidra reference)
        id: parity
        # Reliability telemetry: capped by PARITY_LIMIT on ranking path.
        # Does not block ranking publish (see Commit results); soft-fail here so
        # wall-clock and flake from parity do not hold Pages updates.
        if: always() && steps.fission-gate.outcome != 'skipped'
        continue-on-error: true
        timeout-minutes: 45
        env:
          PARITY_CANONICALIZE_MODE: strict
          RELIABILITY_POLICY: conservative
          PARITY_BINARY_WORKERS: ${{ env.PARITY_BINARY_WORKERS }}
          GHIDRA_HEADLESS_WORKERS: ${{ env.GHIDRA_HEADLESS_WORKERS }}
          FISSION_CLI_WORKERS: ${{ env.FISSION_CLI_WORKERS }}
          FISSION_HOST_PORT: "8000"
        run: |
          set -euo pipefail
          echo "Reliability policy: conservative (strict only, no leniency)"
          echo "PARITY_CANONICALIZE_MODE=$PARITY_CANONICALIZE_MODE"
          echo "PARITY_LIMIT=${PARITY_LIMIT:-0} (0=full)"

          EXTRA=()
          if [ -n "${PARITY_LIMIT:-}" ] && [ "$PARITY_LIMIT" != "0" ]; then
            EXTRA+=(--limit "$PARITY_LIMIT")
          fi
          python -m runner.run_parity \
            --corpus "${CORPUS_SPLIT:-dev}" \
            --decompilers "$PARITY_DECOMPILERS" \
            --request-timeout "$PARITY_REQUEST_TIMEOUT" \
            "${EXTRA[@]}"
          python -m benchmark.telemetry.aggregate \
            --output results/telemetry/latest.json \
            --dashboard-copy public/parity-telemetry.json

          # Assert aggregate recorded strict mode (guards accidental env drift).
          python - <<'PY'
          import json, sys
          t = json.load(open("results/telemetry/latest.json", encoding="utf-8"))
          mode = t.get("canonicalize_mode")
          policy = t.get("reliability_policy")
          if mode != "strict":
              print(f"FAIL telemetry canonicalize_mode={mode!r}, require strict", file=sys.stderr)
              sys.exit(1)
          if policy not in (None, "conservative", "strict"):
              print(f"FAIL reliability_policy={policy!r}", file=sys.stderr)
              sys.exit(1)
          print(f"OK mode={mode} policy={policy} rows={t.get('total_rows')}")
          PY

          # Conservative gates — explicit thresholds, hard-fail.
          # --min-comparable-fd / --min-primary-comparable-fd: function_discovery
          # is scored per-binary (not per-function), so its comparable row count
          # equals the number of unique binaries in the run, not PARITY_LIMIT.
          python scripts/check_parity_smoke.py results/telemetry/latest.json \
            --require-strict \
            --min-comparable 5 \
            --min-comparable-fd 1 \
            --min-usable-coverage 0.90 \
            --max-fetch-error-rate 0.10
          python scripts/check_reliability.py results/telemetry/latest.json \
            --require-strict \
            --min-primary-coverage 0.95 \
            --max-fetch-error-rate 0.05 \
            --max-global-fetch-error-rate 0.05 \
            --min-primary-comparable 5 \
            --min-primary-comparable-fd 1

          # Golden canaries required (not optional).
          test -f benchmark/golden_repros/manifest.json
          python -m benchmark.golden_repros.run benchmark/golden_repros/manifest.json \
            --timeout "$PARITY_REQUEST_TIMEOUT"

          {
            echo "### Parity reliability"
            echo
            echo "- mode: \`$PARITY_CANONICALIZE_MODE\`"
            echo "- policy: \`$RELIABILITY_POLICY\`"
            echo "- limit: \`${PARITY_LIMIT:-0}\` (0=full)"
            if [ -f results/telemetry/latest.json ]; then
              python - <<'PY'
          import json
          t=json.load(open("results/telemetry/latest.json"))
          pub=t.get("publishable") or {}
          dual=(t.get("stages") or {}).get("pcode_parity", {}).get("dual") or pub.get("pcode_dual") or {}
          fd=(t.get("stages") or {}).get("function_discovery", {}).get("dual") or {}
          print(f"- headline_match_rate: `{pub.get('match_rate_comparable')}`")
          print(f"- pcode opcode/loose/strict: `{dual.get('opcode_sequence_match_rate')}` / `{dual.get('loose_full_match_rate')}` / `{dual.get('strict_full_match_rate')}`")
          print(f"- fd presence/manifest recall: `{fd.get('mean_presence_recall')}` / `{fd.get('mean_manifest_recall')}`")
          PY
            fi
          } >> "$GITHUB_STEP_SUMMARY"

      - name: Upload parity telemetry artifacts
        if: always() && steps.parity.outcome != 'skipped'
        uses: actions/upload-artifact@v4
        with:
          name: parity-telemetry
          path: |
            results/telemetry/latest.json
            public/parity-telemetry.json
            results/assembly_parity/latest.jsonl
            results/pcode_parity/latest.jsonl
            results/cfg_parity/latest.jsonl
            results/function_discovery/latest.jsonl
            results/golden_repros/latest.jsonl
          if-no-files-found: warn

      - name: Dashboard data gate (non-empty multi-decomp UI)
        if: always()
        run: |
          set -euo pipefail
          # Multi-decomp UI sources only (public/ + remote). Do NOT require
          # results/dev_latest.json here: ranking runs write 2-tool envelopes that
          # fail min_decompilers=8 and would false-negative this gate.
          python scripts/check_dashboard_data.py \
            --min-rows 1 \
            --min-decompilers 8 \
            --require-valid \
            --require-local \
            --check-remote \
            --local-paths public/benchmark-latest.json \
            --remote-paths \
              https://raw.githubusercontent.com/sjkim1127/fission-benchmark/main/public/benchmark-latest.json \
              https://raw.githubusercontent.com/sjkim1127/fission-benchmark/main/results/dev_latest.json
          # This-run structural sanity (not the multi-decomp UI contract).
          # Validity/publishability is owned by "Validate Fission coverage gate".
          if [ -f results/dev_latest.json ]; then
            python - <<'PY'
          import json, sys
          from pathlib import Path
          p = Path("results/dev_latest.json")
          d = json.loads(p.read_text())
          rows = d.get("rows") or []
          n = len(rows)
          valid = (d.get("validity") or {}).get("valid")
          tools = sorted({str(r.get("decompiler")) for r in rows if r.get("decompiler")})
          if n < 1:
              print(f"FAIL this-run dev_latest has rows={n}", file=sys.stderr)
              sys.exit(1)
          if len(tools) < 2:
              print(f"FAIL this-run dev_latest tools={tools} (need ≥2)", file=sys.stderr)
              sys.exit(1)
          if len(tools) < 8:
              print(
                  f"OK ranking-path this-run rows={n} tools={tools} valid={valid} "
                  f"(multi-decomp UI is separate; validity is fission-gate owned)"
              )
          else:
              # Multi-decomp this-run must be measurement-valid for the UI envelope path.
              if valid is not True:
                  print(f"FAIL multi-decomp this-run valid={valid}", file=sys.stderr)
                  sys.exit(1)
              print(f"OK multi-decomp this-run rows={n} valid={valid} tools={tools}")
          PY
          fi

      - name: Write benchmark summary
        if: always()
        run: |
          {
            echo "## Fission Benchmark"
            echo
            echo "- Fission version: \`$FISSION_VERSION\`"
            echo "- Corpus: \`$CORPUS_SPLIT\`"
            echo "- Decompilers: \`$BENCHMARK_DECOMPILERS\`"
            echo "- Benchmark limit: \`$BENCHMARK_LIMIT\`"
            echo "- Benchmark variant limit: \`$BENCHMARK_VARIANT_LIMIT\`"
            echo "- Parity limit: \`${PARITY_LIMIT:-0}\` (0=full)"
            echo "- Parity mode: \`$PARITY_CANONICALIZE_MODE\` / policy \`$RELIABILITY_POLICY\`"
            echo
            if [ -f results/latest.md ]; then
              sed -n '1,220p' results/latest.md
            else
              echo "No benchmark report was generated."
            fi
          } >> "$GITHUB_STEP_SUMMARY"

      - name: Upload benchmark artifacts
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: benchmark-results-${{ github.run_id }}
          path: |
            results/
            docs/
          if-no-files-found: warn

      # -----------------------------------------------------------------------
      # Dashboard display envelope — always refresh multi-decomp UI data when
      # measurement is valid, even if not fully publishable. This is what Vercel
      # loads (results/dev_latest.json fallback). Without this step operators
      # must manually re-run multi-decomp and force-add the JSON.
      # -----------------------------------------------------------------------
      - name: Update dashboard multi-decomp envelope
        id: dashboard-envelope
        # Only refresh multi-decomp UI when this run actually measured ≥8 tools.
        # Ranking-only (fission+ghidra) must not clobber the multi-tool envelope.
        if: >-
          always() &&
          steps.fission-gate.outcome == 'success' &&
          env.MEASUREMENT_VALID == 'true'
        run: |
          set -euo pipefail
          test -f results/dev_latest.json

          # Ranking-only runs (fission+ghidra) must not clobber multi-tool UI data.
          tool_count="$(python - <<'PY'
          import json
          from pathlib import Path
          d = json.loads(Path("results/dev_latest.json").read_text(encoding="utf-8"))
          tools = {str(r.get("decompiler")) for r in (d.get("rows") or []) if r.get("decompiler")}
          print(len(tools))
          PY
          )"
          if [ "${tool_count}" -lt 8 ]; then
            echo "SKIP multi-decomp envelope refresh: tool_count=${tool_count} (need ≥8)"
            echo "committed=false" >> "$GITHUB_OUTPUT"
            exit 0
          fi

          python - <<'PY'
          import json
          import os
          from pathlib import Path
          from runner.standard_summary import attach_summary_to_envelope

          path = Path("results/dev_latest.json")
          env = json.loads(path.read_text(encoding="utf-8"))
          attach_summary_to_envelope(env)
          path.write_text(json.dumps(env, indent=2) + "\n", encoding="utf-8")

          rows = env.get("rows") or []
          tools = sorted({str(r.get("decompiler")) for r in rows if r.get("decompiler")})
          print(f"dashboard envelope rows={len(rows)} tools={len(tools)} {tools}")
          summary = env.get("summary") or {}
          extensions = summary.get("extensions") or {}
          bare = (extensions.get("bare_compile") or {}).get("by_decompiler") or {}
          tracks = (extensions.get("tracks") or {}).get("by_track") or {}
          readability = (extensions.get("readability_axis") or {}).get("by_decompiler") or {}
          print(
              "quality_extensions "
              f"bare_tools={len(bare)} readability_tools={len(readability)} "
              f"tracks={sorted(tracks)}"
          )
          step_summary = os.environ.get("GITHUB_STEP_SUMMARY")
          if step_summary:
              lines = [
                  "### Quality extensions (non-ranking)",
                  "",
                  f"- bare-compile tools: `{len(bare)}`",
                  f"- readability tools: `{len(readability)}`",
                  f"- tracks: `{', '.join(sorted(tracks)) or 'none'}`",
                  "",
                  "Dashboard: `/quality` — never mixed into semantic ranking.",
                  "",
              ]
              with open(step_summary, "a", encoding="utf-8") as fh:
                  fh.write("\n".join(lines))
          public = Path("public/benchmark-latest.json")
          public.parent.mkdir(parents=True, exist_ok=True)
          public.write_text(path.read_text(encoding="utf-8"), encoding="utf-8")
          print(f"Wrote {public} ({public.stat().st_size} bytes)")
          PY

          python scripts/check_dashboard_data.py \
            --min-rows 1 \
            --min-decompilers 8 \
            --require-valid \
            --require-local

          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git pull --rebase --autostash origin main || true
          git add -f results/dev_latest.json public/benchmark-latest.json
          if git diff --staged --quiet; then
            echo "Dashboard envelope unchanged — no commit"
            echo "committed=false" >> "$GITHUB_OUTPUT"
          else
            git commit -m "chore: refresh multi-decomp dashboard envelope [skip ci]"
            git push
            echo "committed=true" >> "$GITHUB_OUTPUT"
            echo "OK committed multi-decomp dashboard envelope"
          fi

      - name: Commit results
        # Ranking publish path: validity + holdout gate only. Parity is telemetry
        # (continue-on-error above) and must not block Pages on ranking runs.
        if: >-
          steps.publish-mode.outputs.should_publish == 'true' &&
          steps.fission-gate.outcome == 'success'
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git pull --rebase --autostash origin main
          git add -f results/ docs/ public/parity-telemetry.json public/benchmark-latest.json
          git diff --staged --quiet || git commit -m "chore: update benchmark results [skip ci]"
          git push

      - name: Dump container logs on failure
        if: failure()
        run: docker compose logs --no-color

      - name: Stop containers
        if: always()
        run: docker compose down --volumes --remove-orphans

  deploy-vercel:
    name: Deploy to Vercel (production)
    needs: run-benchmark
    # Publish only when the benchmark job succeeded (includes parity reliability gate).
    if: >-
      needs.run-benchmark.result == 'success' &&
      needs.run-benchmark.outputs.is_current == 'true' &&
      needs.run-benchmark.outputs.should_publish == 'true'
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v4
        with:
          ref: main  # Pull latest commit (includes committed results)

      - name: Install Vercel CLI
        run: npm install -g vercel@latest

      - name: Pull Vercel project settings
        env:
          VERCEL_TOKEN: ${{ secrets.VERCEL_TOKEN }}
          VERCEL_ORG_ID: ${{ secrets.VERCEL_ORG_ID }}
          VERCEL_PROJECT_ID: ${{ secrets.VERCEL_PROJECT_ID }}
        run: vercel pull --yes --environment=production --token="$VERCEL_TOKEN"

      - name: Build
        env:
          VERCEL_TOKEN: ${{ secrets.VERCEL_TOKEN }}
          VERCEL_ORG_ID: ${{ secrets.VERCEL_ORG_ID }}
          VERCEL_PROJECT_ID: ${{ secrets.VERCEL_PROJECT_ID }}
        run: vercel build --prod --token="$VERCEL_TOKEN"

      - name: Deploy to production
        env:
          VERCEL_TOKEN: ${{ secrets.VERCEL_TOKEN }}
          VERCEL_ORG_ID: ${{ secrets.VERCEL_ORG_ID }}
          VERCEL_PROJECT_ID: ${{ secrets.VERCEL_PROJECT_ID }}
        run: |
          url=$(vercel deploy --prebuilt --prod --token="$VERCEL_TOKEN")
          echo "Deployed to: $url" >> "$GITHUB_STEP_SUMMARY"

      - name: Revalidate dashboard ISR cache
        continue-on-error: true  # Best-effort — don't fail the deploy job on transient ISR errors
        env:
          REVALIDATE_URL: ${{ secrets.DASHBOARD_REVALIDATE_URL }}
          REVALIDATE_SECRET: ${{ secrets.DASHBOARD_REVALIDATE_SECRET }}
        run: |
          curl --fail-with-body \
            --silent --show-error \
            --request POST \
            --header "Authorization: Bearer $REVALIDATE_SECRET" \
            "$REVALIDATE_URL/api/revalidate"
          echo "Dashboard cache revalidated" >> "$GITHUB_STEP_SUMMARY"
